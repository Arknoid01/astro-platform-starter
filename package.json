{
  "name": "astro-rpg",
  "version": "0.1.0",
  "private": true,
  "description": "RPG spatial 2D — outils Capacitor et génération de contenu",
  "scripts": {
    "android": "cap sync && cap run android",
    "cap:sync": "cap sync",
    "generate:plans": "node tools/generator.js",
    "embed:plans": "node tools/embed-plans.js"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.2",
    "@capacitor/core": "^8.0.2",
    "@capacitor/preferences": "^8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.2",
    "phaser": "^3.87.0"
  },
  "capacitor": {
    "platforms": ["android"]
  }
}
